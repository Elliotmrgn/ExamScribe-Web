# import pytest


# -------Lambda function that triggers on upload -------
# Test that lambda handler for uploading pdfs is recieving a event that includes the s3 bucket info

# Test that the urllib parse unquote is working properly

# Test that the pdf is structered properly (maybe includes ToC, chapters are named properly)

# Test that questions can be found

# Test that answers can be found

# Test that there are equal questions to answers